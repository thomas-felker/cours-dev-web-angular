import { ExemplePipePipe } from './exemple-pipe.pipe';

describe('ExemplePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExemplePipePipe();
    expect(pipe).toBeTruthy();
  });
});
